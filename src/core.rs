//! Re-exports of the shared chess primitives defined in [`crate::types`].
//!
//! See the [`types`](crate::types) module for the full type-level
//! documentation; this module exists so call sites can keep using the
//! familiar `crate::core::*` path.

pub use crate::types::*;
